# Data Management

The data layer provides SQLite and vector-store support for local application workflows.

```python
from data import SQLite

db = SQLite()
tables = db.tables
```

Review the API reference for exact methods, return types, and exception behavior.
