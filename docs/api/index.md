# API Reference

The API reference is generated from the source modules using `mkdocstrings` and Google-style docstrings.

Each page below uses the `:::` directive to render public classes, functions, methods, properties, annotations, and docstring sections directly from the Python source.
